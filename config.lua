Config = {
    -- Replace the array with [nil] in case you don't want to use that section
    veh = {
        types = { "automobile" }, -- automobile, bike, boat, heli, plane, submarine, trailer, trains
        models = { "bati" }, -- Vehicle model name
    },
    items = { "WEAPON_PISTOL" } -- ox_inventory item name
}